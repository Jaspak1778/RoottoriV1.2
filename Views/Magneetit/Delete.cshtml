@model RoottoriV1._2.Models.Magneetit

@{
    ViewBag.Title = "Suunnittelija";
}

<h3>Tarkista tiedot</h3>

<h3>Poista tietue ? </h3>
<div>

    <hr />
    <dl class="dl-horizontal">

        <dt>@Html.DisplayNameFor(model => model.Magneetti)</dt>
        <dd>@Html.DisplayFor(model => model.Magneetti)</dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <br />
        <div class="container">
            <div class="row">
                <div class="col-auto">
                    <a class="btn button2" data-toggle="tooltip" data-placement="bottom" title="Takaisin" href="javascript:void(0);" onclick="history.go(-1);">Takaisin</a>
                </div>
                <div class="col-auto">
                    <button class="btn btn-danger" data-toggle="tooltip" data-placement="bottom" title="Poista mallilta">Poista</button>
                </div>
            </div>
        </div>
    }
</div>